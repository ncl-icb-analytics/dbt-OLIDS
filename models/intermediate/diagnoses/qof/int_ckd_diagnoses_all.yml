version: 2
models:
  - name: int_ckd_diagnoses_all
    description: 'Chronic kidney disease diagnosis and resolution observations for QOF register management.

      One row per observation using CKD_COD (diagnosis) and CKDRES_COD (resolved) clusters. Age restrictions ≥18 years typically applied downstream.'
    tests:
      - cluster_ids_exist:
          arguments:
            cluster_ids: CKDRES_COD, CKD_COD
