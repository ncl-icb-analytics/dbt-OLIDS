models:
  - name: dq_multiple_current_registrations
    description: 'Patient registration data quality monitoring identifying multiple simultaneous current registrations.

      One row per current registration for persons with multiple registrations including overlapping periods and severity scoring.'
