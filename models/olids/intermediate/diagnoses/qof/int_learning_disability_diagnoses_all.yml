version: 2
models:
  - name: int_learning_disability_diagnoses_all
    description: 'Learning disability diagnosis observations for QOF register management.

      One row per diagnosis observation using LD_COD cluster. Annual health check eligibility typically ≥14 years.'
    tests:
      - cluster_ids_exist:
          arguments:
            cluster_ids: LD_COD
