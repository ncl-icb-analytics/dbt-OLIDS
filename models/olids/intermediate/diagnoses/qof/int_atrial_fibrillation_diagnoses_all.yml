version: 2
models:
  - name: int_atrial_fibrillation_diagnoses_all
    description: 'Atrial fibrillation diagnosis and resolution observations for QOF register management.

      One row per observation using AFIB_COD (diagnosis) and AFIBRES_COD (resolved) clusters.'
    tests:
      - cluster_ids_exist:
          arguments:
            cluster_ids: AFIBRES_COD, AFIB_COD
