version: 2
models:
  - name: int_chd_diagnoses_all
    description: 'Coronary heart disease diagnosis observations for QOF register management.

      One row per diagnosis observation using CHD_COD cluster. No resolution codes as this is a lifelong condition.'
    tests:
      - cluster_ids_exist:
          arguments:
            cluster_ids: CHD_COD
