version: 2
models:
  - name: int_nafld_diagnoses_all
    description: 'Non-Alcoholic Fatty Liver Disease diagnosis observations for liver health assessment.

      One row per NAFLD diagnosis observation using hardcoded SNOMED concepts (pending NAFLD_COD cluster). Tracks all diagnosis dates per patient.'
