version: 2
models:
  - name: dim_practice
    description: 'Practice details and organisational hierarchy for North Central London (STPCode = QMJ).

      One row per practice combining Dictionary practice data with OLIDS operational details via INNER JOIN on practice codes.'
