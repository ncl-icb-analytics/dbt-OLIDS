version: 2
models:
  - name: fct_organisation_active_patients
    description: 'Current active patient count per non-obsolete organisation for capacity planning.

      One row per organisation with active patient list size calculated from active patient dimension.'
