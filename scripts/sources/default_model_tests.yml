# Default tests to apply to all staging models
# These tests will survive rebuilding of staging models

default_tests:
  - all_source_columns_in_staging

# Additional model-specific tests can be defined here
# and applied to specific models by name pattern or other criteria
model_specific_tests:
  # Model-specific tests removed due to column name case sensitivity issues
  # Use the all_source_columns_in_staging test for comprehensive validation